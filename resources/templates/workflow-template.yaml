apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: temp
  annotations:
    workflows.argoproj.io/description: |
      temp
    workflows.argoproj.io/version: '>= 3.1.0'
spec:
  entrypoint: main
  templates:

    #templates

    - name: main
      dag:
        tasks:
          []
          #tasks